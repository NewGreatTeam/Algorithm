### 16/5/13 去掉字符串中连续出现k个'0'的子串 ###
	题目：
		给定一个字符串str和一个整数k，如果str中正好有连续的k
		个'0'字符出现时，把k个连续的'0'去除，返回处理后的字符串。
	例子：
		str = "A00B" k = 2,返回"A**B"
		str = "A0000B000" k = 3,返回"A0000B***"
	延伸：
		如果去掉位置不用*替换，而是直接去掉，有什么好方法？
